import Highway from '@dogstudio/highway';
class CustomRenderer extends Highway.Transition {
        // Hooks/methods
        onEnter() {
            // [...]
        }
        onLeave() {
            // [...]
        }
        onEnterCompleted() {
            // [...]
        }
        onLeaveCompleted() {
            // [...]
        }
    }
    // Export Renderer
export default CustomRenderer;